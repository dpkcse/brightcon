<div class="empty-state">
    <div class="empty-state-icon mb-3">{{ $icon ?? '—' }}</div>
    <h2 class="h4">{{ $title ?? 'No content available' }}</h2>
    <p class="mb-0">{{ $message ?? 'Please check back soon for updated information.' }}</p>
</div>
