@extends('frontend.layouts.app')
@php($companyName = $siteSettings?->company_name ?: config('app.name'))
@section('title', 'Contact | '.$companyName)
@section('meta_description', 'Contact '.$companyName.' for construction and engineering inquiries.')
@section('content')
@include('frontend.partials.page-header', ['title' => 'Contact Us', 'description' => 'Send project inquiries, requests for quotation, or general messages to our team.'])
<section class="container section-spacing"><div class="row g-5"><div class="col-lg-5"><div class="contact-panel rounded-4 p-4 h-100"><h2 class="h4 mb-4">Company Information</h2>@if($siteSettings?->address)<p><strong>Address:</strong><br>{{ $siteSettings->address }}</p>@endif @if($siteSettings?->phone)<p><strong>Phone:</strong><br><a href="tel:{{ $siteSettings->phone }}">{{ $siteSettings->phone }}</a></p>@endif @if($siteSettings?->email)<p><strong>Email:</strong><br><a href="mailto:{{ $siteSettings->email }}">{{ $siteSettings->email }}</a></p>@endif @if($socialLinks->isNotEmpty())<div class="d-flex gap-3 flex-wrap mt-4">@foreach($socialLinks as $social)@continue(blank($social->url))<a href="{{ $social->url }}" target="_blank" rel="noopener" class="social-pill" aria-label="{{ $social->platform }}">{{ $social->platform }}</a>@endforeach</div>@endif<div class="map-placeholder mt-4">Map or embedded location can be connected from future CMS settings.</div></div></div><div class="col-lg-7"><div class="contact-panel rounded-4 p-4 p-lg-5">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<h2 class="h4 mb-4">Send a Message</h2>@include('frontend.partials.contact-form')</div></div></div></section>
@endsection
