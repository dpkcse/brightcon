@extends('installer.layout') @section('content')<h1>Review</h1>@foreach(['app_name'=>'Application','app_url'=>'URL','db_host'=>'Database host','db_port'=>'Database port','db_name'=>'Database name','db_user'=>'Database user','admin_name'=>'Administrator','admin_email'=>'Administrator email','seed'=>'Data mode'] as $key=>$label)<div class="row"><span>{{ $label }}</span><strong>{{ $summary[$key] }}</strong></div>@endforeach<p class="secret">Database password, administrator password, application key, and environment contents are not displayed or retained.</p><form method="post" action="{{ route('install.process') }}">@csrf @foreach($summary as $key=>$value)<input type="hidden" name="{{ $key }}" value="{{ $value }}">@endforeach<input type="hidden" name="storage_link" value="1"><label for="db_password">Re-enter database password</label><input id="db_password" type="password" name="db_password" required><label for="admin_password">Re-enter administrator password</label><input id="admin_password" type="password" name="admin_password" required><label for="admin_password_confirmation">Confirm administrator password</label><input id="admin_password_confirmation" type="password" name="admin_password_confirmation" required>@if(is_file(base_path('.env')))<label><input style="width:auto" type="checkbox" name="approve_env_update" value="1" required> I approve backing up and updating the existing environment file.</label>@endif<label><input style="width:auto" type="checkbox" name="confirm" value="1" required> I confirm this database is intended for a new/recoverable Buildora installation.</label><button>Run installation</button></form>@endsection
