@extends('installer.layout') @section('content')<h1>Directory permissions</h1>@foreach($items as $item)<div class="row"><span>{{ $item['path'] }}</span><strong class="{{ $item['passed'] ? 'ok':'fail' }}">{{ $item['passed'] ? 'Writable':'Not writable' }}</strong></div>@endforeach<p>Public storage: <strong>{{ $storageStatus }}</strong>. A failed optional link will not invalidate installation.</p>@if(collect($items)->every('passed'))<a class="button" href="{{ route('install.application') }}">Configure installation</a>@else<p class="fail">Set PHP write access without changing ownership unnecessarily.</p>@endif @endsection
