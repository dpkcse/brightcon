@extends('installer.layout') @section('content')<h1>Server requirements</h1>@foreach($items as $item)<div class="row"><span>{{ $item['name'] }} — {{ $item['required'] }}</span><strong class="{{ $item['passed'] ? 'ok':'fail' }}">{{ $item['detected'] }}{{ !$item['blocking'] ? ' (recommended)':'' }}</strong></div>@endforeach @if($passes)<a class="button" href="{{ route('install.permissions') }}">Check permissions</a>@else<p class="fail">Resolve blocking requirements before continuing.</p>@endif @endsection
