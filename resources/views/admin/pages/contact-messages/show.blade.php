@extends('admin.layouts.app')
@section('title','Contact Message')
@section('page-heading','Contact Message Details')
@section('content')
<div class="card border-0 shadow-sm"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-3">Full name</dt><dd class="col-sm-9">{{ $message->full_name }}</dd><dt class="col-sm-3">Email</dt><dd class="col-sm-9"><a href="mailto:{{ $message->email }}">{{ $message->email }}</a></dd><dt class="col-sm-3">Phone</dt><dd class="col-sm-9">{{ $message->phone ?? '—' }}</dd><dt class="col-sm-3">Subject</dt><dd class="col-sm-9">{{ $message->subject ?? '—' }}</dd><dt class="col-sm-3">Message</dt><dd class="col-sm-9"><div class="border rounded p-3 bg-light">{{ $message->message }}</div></dd><dt class="col-sm-3">IP address</dt><dd class="col-sm-9">{{ $message->ip_address ?? '—' }}</dd><dt class="col-sm-3">User agent</dt><dd class="col-sm-9">{{ $message->user_agent ?? '—' }}</dd><dt class="col-sm-3">Created date</dt><dd class="col-sm-9">{{ $message->created_at?->format('M d, Y H:i') }}</dd><dt class="col-sm-3">Status</dt><dd class="col-sm-9"><span class="badge {{ $message->is_read ? 'text-bg-secondary' : 'text-bg-danger' }}">{{ $message->is_read ? 'Read' : 'Unread' }}</span></dd></dl></div><div class="card-footer bg-white d-flex gap-2 justify-content-between"><a class="btn btn-outline-secondary" href="{{ route('admin.contact-messages.index') }}">Back</a><div><form class="d-inline" method="POST" action="{{ $message->is_read ? route('admin.contact-messages.mark-unread',$message) : route('admin.contact-messages.mark-read',$message) }}">@csrf @method('PATCH')<button class="btn btn-outline-primary-brand">{{ $message->is_read ? 'Mark unread' : 'Mark read' }}</button></form><form class="d-inline" method="POST" action="{{ route('admin.contact-messages.destroy', $message) }}" onsubmit="return confirm('Delete this message?')">@csrf @method('DELETE')<button class="btn btn-outline-danger">Delete</button></form></div></div></div>
@endsection
