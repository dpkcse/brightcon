@extends('admin.layouts.app')
@section('title','Contact Messages')
@section('page-heading','Contact Messages')
@section('content')
<div class="card border-0 shadow-sm"><div class="card-header bg-white"><form class="row g-2 align-items-end"><div class="col-md-6"><label class="form-label">Search</label><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Name, email, phone, subject, message"></div><div class="col-md-3"><label class="form-label">Status</label><select class="form-select" name="status"><option value="">All</option><option value="unread" @selected(request('status')==='unread')>Unread</option><option value="read" @selected(request('status')==='read')>Read</option></select></div><div class="col-md-3"><button class="btn btn-primary-brand">Filter</button><a class="btn btn-outline-secondary" href="{{ route('admin.contact-messages.index') }}">Reset</a></div></form></div><div class="card-body p-0"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Subject</th><th>Status</th><th>Date</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($messages as $message)<tr><td>{{ $message->full_name }}</td><td>{{ $message->email }}</td><td>{{ $message->phone ?? '—' }}</td><td>{{ $message->subject ?? '—' }}</td><td><span class="badge {{ $message->is_read ? 'text-bg-secondary' : 'text-bg-danger' }}">{{ $message->is_read ? 'Read' : 'Unread' }}</span></td><td>{{ $message->created_at?->format('M d, Y H:i') }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary-brand" href="{{ route('admin.contact-messages.show', $message) }}">View</a><form class="d-inline" method="POST" action="{{ $message->is_read ? route('admin.contact-messages.mark-unread',$message) : route('admin.contact-messages.mark-read',$message) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">{{ $message->is_read ? 'Mark unread' : 'Mark read' }}</button></form><form class="d-inline" method="POST" action="{{ route('admin.contact-messages.destroy', $message) }}" onsubmit="return confirm('Delete this message?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No contact messages found.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-white">{{ $messages->links() }}</div></div>
@endsection
