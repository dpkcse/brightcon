@extends('admin.layouts.app')
@section('title','Gallery Images')
@section('page-heading','Gallery Images')
@section('content')
<div class="card border-0 shadow-sm"><div class="card-header bg-white d-flex justify-content-between align-items-center"><strong>Gallery Images</strong><a class="btn btn-primary-brand btn-sm" href="{{ route('admin.gallery-images.create') }}">Add New</a></div><div class="card-body"><form class="row g-2"><div class="col-md-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search title or category"></div><div class="col-md-3"><select class="form-select" name="category"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category }}" @selected($category===request('category'))>{{ $category }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="status"><option value="">All statuses</option><option value="1" @selected(request('status')==='1')>Active</option><option value="0" @selected(request('status')==='0')>Inactive</option></select></div><div class="col-md-3"><button class="btn btn-outline-primary-brand">Filter</button><a class="btn btn-outline-secondary" href="{{ route('admin.gallery-images.index') }}">Reset</a></div></form></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Title</th><th>Image</th><th>Category</th><th>Sort</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->title ?? '—' }}</td><td><img src="{{ Storage::url($item->image) }}" style="width:80px;height:48px;object-fit:cover" class="rounded" alt=""></td><td>{{ $item->category ?? '—' }}</td><td>{{ $item->sort_order }}</td><td><span class="badge {{ $item->status ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $item->status ? 'Active' : 'Inactive' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary-brand" href="{{ route('admin.gallery-images.edit',$item) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.gallery-images.destroy',$item) }}" onsubmit="return confirm('Delete this gallery image?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">No gallery images found.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $items->links() }}</div></div>
@endsection
