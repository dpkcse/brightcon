@extends('admin.layouts.app')
@section('page-heading','Custom Pages')
@section('content')
<div class="d-flex justify-content-between mb-3"><form><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search pages"></form><a class="btn btn-primary-brand" href="{{ route('admin.pages.create') }}">Add page</a></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Title</th><th>Slug</th><th>Status</th><th>Featured</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->title }}</td><td>{{ $item->slug }}</td><td><span class="badge text-bg-{{ $item->status==='published'?'success':'secondary' }}">{{ ucfirst($item->status) }}</span></td><td>{{ $item->is_featured?'Yes':'No' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.pages.preview',$item) }}">Preview</a> <a class="btn btn-sm btn-outline-primary" href="{{ route('admin.pages.edit',$item) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.pages.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-4">No custom pages yet.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $items->links() }}</div></div>
@endsection
