@extends('admin.layouts.app')
@section('title','Feature Highlights')
@section('page-heading','Feature Highlights')
@section('content')
<div class="card border-0 shadow-sm"><div class="card-header bg-white d-flex justify-content-between align-items-center"><strong>Feature Highlights</strong><a class="btn btn-primary-brand btn-sm" href="{{ route('admin.features.create') }}">Add New</a></div><div class="card-body p-0"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Title</th><th>Icon</th><th>Image</th><th>Sort</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->title }}</td><td>{{ $item->icon_class ?? '—' }}</td><td>@if($item->image)<img src="{{ Storage::url($item->image) }}" alt="" style="width:80px;height:48px;object-fit:cover" class="rounded">@else — @endif</td><td>{{ $item->sort_order }}</td><td><span class="badge {{ $item->status ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $item->status ? 'Active' : 'Inactive' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary-brand" href="{{ route('admin.features.edit', $item) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.features.destroy', $item) }}" onsubmit="return confirm('Delete this feature?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">No features found.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-white">{{ $items->links() }}</div></div>
@endsection
