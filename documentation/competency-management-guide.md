# Competency management
Use **Content → Competencies** to publish ordered capability cards at the unchanged `/competency` route. Icons and raster images are optional. No artificial percentage is stored. A temporary static fallback remains until managed records are published. Delete actions archive records.
