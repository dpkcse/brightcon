# Contact messages
Valid throttled, honeypot-protected submissions are stored before notification. Admins can filter, read/unread, reply, archive, and keep an internal note. Reply recipients are fixed to the validated original visitor address; the configured application sender is used. Transport exceptions are never shown or stored. Deletion is a non-destructive archive workflow.
