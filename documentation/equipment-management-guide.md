# Equipment management
Use **Content → Equipment** for website capability records, not accounting or maintenance. Publish and order records; images are optional raster uploads. `/equipment-list` is unchanged and uses managed published records when present, retaining a small static fallback during migration. Delete actions archive records.
