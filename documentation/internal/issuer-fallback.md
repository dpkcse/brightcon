# Issuer fallback

Normal issuance should use the Naxas portal only after deployment acceptance. The separately controlled owner issuer remains the emergency outage, offline enterprise, development and test fallback and must remain excluded from buyer packages with every private/test signing key. This checkout does not contain `owner-tools/`; do not reconstruct or distribute it from buyer CMS code.
